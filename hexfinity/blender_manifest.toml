schema_version = "1.0.0"

id = "hexfinity"
version = "0.1.0"
name = "HexFinity"
tagline = "Modular hexagonal terrain tiles for tabletop miniatures"
maintainer = "Andre Tischer <hexfinity@tischer.dk>"
type = "add-on"
blender_version_min = "5.1.0"
license = ["SPDX:MIT"]
tags = ["Mesh", "Add Mesh"]

[permissions]
files = "Reads and writes settings.json (the Path Segment authoring tool's type/segment library) in the extension's per-user data directory; 'Export + Slice' writes STL/manifest/G-code files to a user-chosen folder and invokes a locally installed Bambu Studio executable to slice them"
